services:
  btg-baixa-api:
    build: .
    container_name: btg-baixa-api
    restart: unless-stopped
    ports:
      - "3333:3333"
    env_file:
      - .env
    volumes:
      - ./storage:/app/storage
      - ./logs:/app/logs
    environment:
      NODE_ENV: production